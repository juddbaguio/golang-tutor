package main

// 1.) Compare first two elements to check if they are sorted and so on.
// 2.) Compare until all are sorted
// func BubbleSort(intSlice []int) []int {
// 	var lastIndex int = len(intSlice) - 1
// 	var swapped bool

// 	for i := 0; i <= lastIndex; i++ {
// 		for j := 0; j <= lastIndex-1; j++ {
// 			if intSlice[j] > intSlice[j+1] {
// 				intSlice[j], intSlice[j+1] = intSlice[j+1], intSlice[j]
// 				swapped = true
// 			}
// 		}

// 		swapped = false
// 	}

// 	return intSlice
// }
